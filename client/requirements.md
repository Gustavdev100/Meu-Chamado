## Packages
date-fns | Formatting dates for ticket creation
lucide-react | Icons for the UI

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
Assuming standard Shadcn UI components are available as listed in the context.
API uses /api/tickets endpoint.
